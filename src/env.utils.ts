export const BASE_URL = __ENV.BASE_URL || __ENV.ROOT_URL || "http://localhost:8090";

export const CLIENT_ID = __ENV.CLIENT_ID || "clientId";
export const CLIENT_SECRET = __ENV.CLIENT_SECRET || "clientSecret";
export const CLIENT_SCOPE = __ENV.CLIENT_SCOPE || "timeline userbook blog lvs actualites pronote schoolbook support viescolaire zimbra conversation directory homeworks userinfo workspace portal cas sso presences incidents competences diary edt infra auth";
export const CLIENT_CODE = __ENV.CLIENT_CODE || "OAUTH_CODE";
